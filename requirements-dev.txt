# Development dependencies (local testing of the overlay).
# The platform Docker build gets autogen deps via deploy/customize.py.
autogen-agentchat==0.4.7
autogen-ext[openai]==0.4.7
pytest>=8
pytest-asyncio>=0.24
